# ~/.tmuxinator/sky.yml

name: sky
root: ~/code/chef-solo

pre: vagrant up

windows:
  - editor:
      layout: main-vertical
      panes:
        - VM:
          - vagrant ssh
          - cd /var/lamp/code
          - clear
        - Editor:
          - cd ../
          - vim

